import encoder from './lib/encoder'
import { mode, objectFit, encoderOptions, pcmFormat } from './lib/types'

export default encoder
export {
    mode,
    objectFit,
    encoderOptions,
    pcmFormat
}